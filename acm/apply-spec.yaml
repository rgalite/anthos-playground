# apply-spec.yaml

applySpecVersion: 1
spec:
  configSync:
    # Set to true to install and enable Config Sync
    enabled: true
    sourceFormat: hierarchy
    syncRepo: https://github.com/rgalite/anthos-playground.git
    syncBranch: main
    secretType: none
    policyDir: acm/config
